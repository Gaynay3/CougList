<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>CougList - Browse Listings</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link rel="stylesheet" href="/css/style.css"/>
</head>
<body>

<header class="coug-crimson text-white d-flex justify-content-between align-items-center p-4 flex-wrap">
  <h1 class="m-0">
    <a href="/" style="text-decoration: none; color: inherit;">CougList</a>
  </h1>
  <nav>
    <a href="/" class="mx-3 fw-bold text-white">Home</a>
    <a href="/dashboard" class="mx-3 fw-bold text-white">Dashboard</a>
    <a href="/about-us" class="mx-3 fw-bold text-white">About</a>
  </nav>
</header>

<div class="container my-5">
  <div class="row g-4">
    <% listings.forEach(listing => { %>
      <div class="col-md-6 col-lg-4">
        <div class="card h-100 shadow-sm">
          <img src="/imgs/<%= listing.image %>" class="card-img-top" alt="<%= listing.title %>">
          <div class="card-body">
            <h5 class="card-title text-danger fw-bold"><%= listing.title %></h5>
            <p class="card-text">
              <strong>Price:</strong> $<%= listing.price %><br>
              <strong>Location:</strong> <%= listing.location %>
            </p>
            <a href="/listings/<%= listing.id %>" class="btn btn-danger w-100">View Details</a>
          </div>
        </div>
      </div>
    <% }) %>
  </div>
</div>

<footer class="coug-crimson text-white text-center p-3 mt-5">
  &copy; 2025 CougList - Washington State University
</footer>

</body>
</html>
